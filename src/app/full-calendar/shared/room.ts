export class Room {
  id: number;
  name: string;
  btnColor: string;

  constructor(id: number, name: string, btnColor: string) {
    this.id = id;
    this.name = name;
    this.btnColor = btnColor;
  }
}
